# Bachelor thesis experiment

### Need to change the hardcoded paths inside the scripts before running on your local machine.
### The number of github tokens can change the time needed for data fetching
